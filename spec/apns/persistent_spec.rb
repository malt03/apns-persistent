require 'spec_helper'

describe Apns::Persistent do
  it 'has a version number' do
    expect(Apns::Persistent::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
